var MyApp = (function($) {
  var module = {
    init: function init() {
      // initialisation goes here...
    } // module.init()
  };
  
  $(document).ready(module.init);

  return module;
}(jQuery));
